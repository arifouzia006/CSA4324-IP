<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Web</title>
    <style>
        /* Basic CSS styling to mimic WhatsApp Web appearance */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }
        .header {
            padding: 10px 0;
            border-bottom: 1px solid #ddd;
            text-align: center;
        }
        .chat {
            padding: 20px 0;
            border-bottom: 1px solid #ddd;
        }
        .chat .sender {
            font-weight: bold;
        }
        .chat .message {
            margin-top: 5px;
        }
        .input-area {
            padding-top: 20px;
            text-align: center;
        }
        .input-area input[type="text"] {
            width: 80%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 20px;
            outline: none;
        }
        .input-area button {
            padding: 10px 20px;
            margin-left: 10px;
            background-color: #25D366;
            color: #fff;
            border: none;
            border-radius: 20px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>WhatsApp Web</h1>
        </div>
        <div class="chat">
            <div class="message">
                <span class="sender">John Doe:</span> Hey there!
            </div>
            <div class="message">
                <span class="sender">Jane Smith:</span> Hi, how are you?
            </div>
            <!-- Add more messages here -->
        </div>
        <div class="input-area">
            <input type="text" placeholder="Type your message...">
            <button>Send</button>
        </div>
    </div>
</body>
</html>
